version: "3.8"
services:
  primo-db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - DATASOURCE_URL=jdbc:postgresql://PostgreSQL:5432/cinema
      - POSTGRES_USER=primo
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=cinema
    restart: unless-stopped